# requirements.txt

# -----------------------------------------------------------------
# Core SDN Controller
# -----------------------------------------------------------------
ryu
# -----------------------------------------------------------------
# AI, Machine Learning, and Data Handling
# -----------------------------------------------------------------
# Main deep learning framework for running .keras models
tensorflow

# Used for scalers, the ExtraTreesClassifier, and metrics.
# Version is pinned to ensure compatibility with your saved .joblib files.
scikit-learn==1.3.2

# Used in the AI pipeline to structure features before scaling
pandas

# Core library for numerical operations (dependency for almost everything)
numpy

# Used specifically for loading/saving scikit-learn models (.joblib files)
joblib

# -----------------------------------------------------------------
# Communication and Utilities
# -----------------------------------------------------------------
# For making HTTP API calls to the Ollama LLM service
requests

# For SSH communication, allowing the controller to apply iptables rules
# on a remote firewall or gateway.
paramiko

